---
name: skill-template
description: One or two sentences. What the skill does, then when to use it, using the concrete words a user would type. Add "Not for X" if a neighbouring topic could mis-trigger it.
---

# Skill Template

One paragraph on what this skill is for and what it assumes is already true.

## When to use this

- Trigger case one
- Trigger case two

Do not use this for: the adjacent thing people confuse it with.

## Steps

1. First step. Put the common path first.
2. Second step.
3. Third step.

## Notes

Gotchas, failure modes, and anything that will surprise someone the first time.

Long reference material — schemas, tables, prompts, output templates — belongs
in `references/` and is linked from here, so it loads only when it is actually
needed. Executable code goes in `scripts/`, files copied into output in
`assets/`.
